"""Steps that assert on PC-side behavior beyond the wire (e.g. logs).

Currently empty — the v1 happy path only asserts on PLC state, which
lives in `plc_steps.py`. Steps for log assertions land alongside the
guard-fail scenario in the next slice.
"""
