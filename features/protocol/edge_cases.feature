Feature: Scan-cycle edge cases (v3 placeholder)

  This file is reserved for the v3 edge cases listed in
  docs/protocol.md (unparseable scan URLs, scans during OS startup,
  duplicate scans across cycles, scan while PC is mid-write). v1 ships
  with the happy paths covered in scan_cycle.feature and active_partij.feature;
  these scenarios will be promoted from notes to executable spec when
  v3 begins. Intentionally empty — behave's runner stays green.
