<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>Bulb Picklist Label</title>
    <style>
        /* Define CSS variables for responsive scaling */
        :root {
            /* Base size for calculations - using the default label size */
            --base-width: 151;
            --base-height: 101;
            
            /* Calculate scaling factors based on actual dimensions */
            --width-scale: calc({{ label_width | replace: 'mm', '' }} / var(--base-width));
            --height-scale: calc({{ label_height | replace: 'mm', '' }} / var(--base-height));
            
            /* Use the smaller scale factor to ensure everything fits */
            --scale: min(var(--width-scale), var(--height-scale));
            
            /* Derived sizes for various elements */
            --title-font-size: calc(24pt * var(--scale));
            --main-font-size: calc(18pt * var(--scale));
            --small-font-size: calc(8pt * var(--scale));
            --main-padding: calc(5mm * var(--scale));
            --small-padding: calc(2mm * var(--scale));
            --qr-size: calc(25mm * var(--scale));
            --qr-top-position: calc(5mm * var(--scale));
            --qr-right-position: calc(5mm * var(--scale));
            --margin-top: calc(2mm * var(--scale));
        }

        @page {
            size: {{ page_size }};
            margin: 0;
        }

        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        .label {
            width: {{ label_width }};
            height: {{ label_height }};
            box-sizing: border-box;
            border: 1px solid #000;
            display: grid;
            grid-template-rows: 33% 33% 33%;
            grid-template-columns: 50% 50%;
        }

        .plant-name {
            grid-column: 1 / 3;
            grid-row: 1;
            font-size: var(--title-font-size);
            font-weight: bold;
            padding: var(--small-padding);
            position: relative;
        }

        .url-text {
            display: block;
            font-size: var(--small-font-size);
            color: #999;
            font-weight: normal;
            margin-top: var(--margin-top);
        }

        .code-left {
            grid-column: 1;
            grid-row: 2;
            font-size: var(--main-font-size);
            padding: var(--main-padding);
            border-top: 1px solid #000;
            border-right: 1px solid #000;
            position: relative;
        }

        .code-right {
            grid-column: 2;
            grid-row: 2;
            font-size: var(--main-font-size);
            padding: var(--main-padding);
            border-top: 1px solid #000;
            position: relative;
        }

        .location {
            grid-column: 1;
            grid-row: 3;
            font-size: var(--main-font-size);
            padding: var(--main-padding);
            border-top: 1px solid #000;
            border-right: 1px solid #000;
            position: relative;
        }

        .trays {
            grid-column: 2;
            grid-row: 3;
            font-size: var(--main-font-size);
            padding: var(--main-padding);
            border-top: 1px solid #000;
            position: relative;
        }

        .qr-code {
            position: absolute;
            top: var(--qr-top-position);
            right: var(--qr-right-position);
            width: var(--qr-size);
            height: var(--qr-size);
        }

        .label-text {
            display: block;
            font-size: var(--small-font-size);
            color: #999;
            margin-top: var(--margin-top);
        }
    </style>
</head>

<body>
    <div class="label">
        <div class="plant-name">
            {{ ras }}
            <span class="url-text">{{ scan_url }}</span>
            <img src="{{ qr_code }}" alt="QR Code" class="qr-code">
        </div>
        <div class="code-left">
            {{ bollen_code }}
            <span class="label-text">Bollencode</span>
        </div>
        <div class="code-right">
            {{ id }}
            <span class="label-text">Oppotpartij</span>
        </div>
        <div class="location">
            {{ locatie }}
            <span class="label-text">Locatie</span>
        </div>
        <div class="trays">
            {{ aantal_bakken }} kratten
            <span class="label-text">Aantal</span>
        </div>
    </div>
</body>

</html>
